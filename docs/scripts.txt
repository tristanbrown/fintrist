### Redis Server ###
# In WSL, start with:
redis-server --daemonize yes
# Interact with:
redis-cli

### Celery Workers on Windows ###
# In venv, start with:
celery -A fintrist worker -E --loglevel=INFO -P solo
# Kill all celery workers with:
taskkill /F /IM celery.exe /T

### Celery Workers on Linux ###
# In venv, start with:
celery -A fintrist worker -E --loglevel=INFO
# Kill all celery workers with:

### Celery Beat ###
# Start with:
celery -A fintrist beat